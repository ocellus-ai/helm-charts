{{- if .Values.service.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: ocellusai-secrets
  namespace: {{ include "ocellusai.namespace" . }}
type: Opaque
stringData:
  # API Key for external services like OpenAI, etc.
  apiKey: {{ .Values.service.apiKey | quote }}
{{- end }}
