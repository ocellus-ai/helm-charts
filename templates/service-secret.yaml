{{- if .Values.service.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: ocellusai-secrets
  namespace: {{ include "ocellusai.namespace" . }}
type: Opaque
stringData:
  API_KEY: {{ .Values.service.apiKey | quote }}
{{- end }}
